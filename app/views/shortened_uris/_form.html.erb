<%= form_for(shortened_uri, url: root_path) do |f| %>
  <% if shortened_uri.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shortened_uri.errors.count, "error") %> prohibited this shortened_uri from being saved:</h2>

      <ul>
      <% shortened_uri.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :original_uri %>
    <%= f.text_field :original_uri, value: 'foo', autofocus: 'autofocus', onfocus: 'this.select();', readonly: true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
